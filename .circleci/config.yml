version: 2.1

jobs:
  build:
    docker:
      - image: cimg/go:1.23
    steps:
      - checkout
      - go mod download
      - go build -o myapp
      - go test ./... -coverprofile=coverage.out
      - store_artifacts:
          path: myapp
      - store_test_results:
          path: coverage.out
