version: 2.1

jobs:
  build:
    docker:
      - image: cimg/go:1.23
    steps:
      - checkout
      - restore_cache:
          keys:
            - go-mod-{{ checksum "go.sum" }}
      - run:
          name: Download dependencies
          command: go mod download
      - save_cache:
          key: go-mod-{{ checksum "go.sum" }}
          paths:
            - /go/pkg/mod
      - run:
          name: Build application
          command: go build -o myapp
      - run:
          name: Check for vulnerabilities
          command: |
            go install golang.org/x/vuln/cmd/govulncheck@latest
            govulncheck ./...
      - persist_to_workspace:
          root: .
          paths:
            - myapp
      - store_artifacts:
          path: myapp

  test:
    docker:
      - image: cimg/go:1.23
    steps:
      - checkout
      - restore_cache:
          keys:
            - go-mod-{{ checksum "go.sum" }}
      - run:
          name: Download dependencies
          command: go mod download
      - run:
          name: Run tests
          command: go test -coverprofile=coverage.out -parallel 4
      - store_test_results:
          path: coverage.out

  deploy_staging:
    docker:
      ###- image: cimg/go:1.23
      - image: cimg/base:stable
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "SHA256:R7nNh/wxX4zR1H7NQJjONekwv+87TnUBqS5dbtPzohY"
      - attach_workspace:
          at: .
      - run:
          name: List workspace contents
          command: ls -l .
      - run:
          name: Deploy to staging server
          command: |
            ###sudo mkdir -p /var/www/myapp
            ###sudo cp ./myapp /var/www/myapp/
            sudo ssh -o StrictHostKeyChecking=no ubuntu@ec2-18-183-144-224.ap-northeast-1.compute.amazonaws.com
            ###scp -o StrictHostKeyChecking=no ./myapp ubuntu@ec2-18-183-144-224.ap-northeast-1.compute.amazonaws.com:/home/ubuntu/myapp
  
      - run:
          name: List myapp
          command: sudo ls -l /var/www/myapp/

workflows:
  build-test-and-deploy:
    jobs:
      - build
      - test:
          requires:
            - build
      - deploy_staging:
          requires:
            - test
          ###filters:
            ###branches:
              ###only:
                ###- main
